name: CI - Build and Test

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build_and_test:
    runs-on: ubuntu-latest

    steps:
      # 1. Repository'i çekme
      - uses: actions/checkout@v2

      # 2. Frontend için Node.js kurulum
      - name: Set up Node.js for Frontend
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      # 3. Frontend bağımlılıklarını kurma
      - name: Install Dependencies for Frontend
        run: |
          cd frontend
          npm install

      # 4. Frontend uygulamasını başlatma
      - name: Start Frontend Application
        run: |
          cd frontend
          npm start &  # Frontend arka planda çalışacak
          sleep 10

      # 5. Backend için Node.js kurulum
      - name: Set up Node.js for Backend
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      # 6. Backend bağımlılıklarını kurma
      - name: Install Dependencies for Backend
        run: |
          cd backend
          npm install

      # 7. Backend uygulamasını başlatma
      - name: Start Backend Application
        run: |
          cd backend
          npm start  # Backend'i ön planda başlatıyoruz
          sleep 10

      # 8. Uygulamanın başlatılmasını bekleme
      - name: Wait for Backend to be ready
        run: |
          timeout=600
          while ! curl --silent --fail http://localhost:3001; do  # Portu 3001 olarak değiştiriyoruz
            echo "Waiting for backend to start..."
            sleep 10
            ((timeout--))
            if [ $timeout -le 0 ]; then
              echo "Timeout reached, backend did not start!"
              exit 1
            fi
          done
          echo "Backend is ready!"

      # 9. Test framework'ünü çalıştırma (Selenium ve Cucumber testleri)
      - name: Run Selenium and Cucumber Tests
        run: mvn clean verify  # Test framework'ünüzü çalıştıran komut

      # 10. Test tamamlandıktan sonra uygulamaları kapatma
      - name: Stop Frontend Application
        run: |
          pkill -f "npm start"  # Frontend uygulamasını kapat

      - name: Stop Backend Application
        run: |
          pkill -f "npm start"  # Backend uygulamasını kapat

